# Commands

Claude Code now treats Skills as the recommended command format. The slash commands for this team live in `.claude/skills/<name>/SKILL.md` and can be invoked as `/team-plan`, `/implement-feature`, `/code-review`, etc.

Legacy `.claude/commands/*.md` files are intentionally not duplicated to avoid divergent command definitions.
